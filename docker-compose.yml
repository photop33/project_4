version: '3'
services:
  project:
    build: .
    ports:
     - "5005:5005"

  redis:
    image: "redis:alpine"
    
  mysql:
    image: "pymysql:lastest"
    build: .
    ports:
    - "4000:4000"
  
